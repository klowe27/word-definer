<h2>Add words</h2>
<form action="/input" method="post">

  <div class="form-group">
    <label for="word_name">Enter a word</label>
    <input type="text" name="word_name" class="form-control">
  </div>

  <div class="form-group">
    <label for="definition">Enter a definition</label>
    <input type="text" name="definition" class="form-control">
  </div>
  <button type="submit" name="button" class="btn btn-primary">Add Word</button>
</form>
<p style="<%= @error %>">Oops, you've already added that word. Try adding a new word.</p>

<div style="<%= @display %>">
  <h2>Your words</h2>
  <ul>
    <% @words.each do |word| %>
      <li><a href="word/<%= word.id %>"><%= word.name %></a></li>
    <% end %>
  </ul>

  <a href="/sort"><button class="btn btn-primary">Sort A-Z</button></a>
  <a href="/reset"><button class="btn btn-primary">Reset</button></a>
</div>
