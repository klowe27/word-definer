<div class="list">
  <div class="row">
    <div class="col-md-6">
      <h2><%= @name %></h2>
      <% @definitions.each_with_index do |definition, index| %>
        <p><strong>Definition <%= index +1 %>:</strong> <%= definition %></p>
      <% end %>
      <form action="/delete" method="post">
        <input style="display: none" type="text" name="id" value="<%= @id %>">
        <button type="submit" name="button" class="btn">Delete Word</button></a>
      </form>
    </div>
    <div class="col-md-6">
      <h3>Add a definition</h3>
      <form action="/definition" method="post">
        <div class="form-group">
          <input type="text" name="definition" class="form-control">
        </div>
        <input style="display:none" type="text" name="id" value="<%= @id %>">
        <button type="submit" name="button" class="btn">Add</button>
      </form>
    </div>
  </div>
</div>
